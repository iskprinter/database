apiVersion: v2
name: mongodb
description: Deploys the MongoDB Community Kubernetes Operator
type: application
version: 0.1.0  # This is the chart version.
appVersion: "0.7.0"  # This is the version number of the application being deployed.
